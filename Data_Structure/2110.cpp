#include <iostream>
#include <queue>

using namespace std;

vector<int> list;
int n, m;

int makeanswer(int n, int m)
{



}

int main(void)
{
    cin >> n >> m;
    int input;
    for(int i=0; i<N; i++)
    {
        cin >> input;
        list.push_back(input);
    }
    sort(list.begin(), list.end());
    cout << makeanswer(n, m);
}
